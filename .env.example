# Путь к файлу SQLite внутри контейнера (том /data)
DATABASE_PATH=/data/app.db

# Секрет Flask (сессии, подпись; для продакшена задать уникальное значение)
FLASK_SECRET_KEY=replace-with-long-random-string
